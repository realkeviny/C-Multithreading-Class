# Exercise 1: std::accumulate

## Briefly describe std::accumulate()

It takes an iterator range, and an initial value.

Add all the elements to the initial value, and return the result.

I can change the behavior by providing my own callable object, and the algorithm will use that to calculate the result.

## Write a program to demonstrate the use of std::accumulate()

## Rewrite your program to use a parallel execution policy



# Exercise 2: std::partial_sum

## Briefly describe std::partial_sum()

Numerical Integration

It will populate the destination vector, using the element of the source vector.

Each element in the destination vector will be the sum of the elements so far in the source vector.

## Write a program to demonstrate its use

## Using a different standard C++ function, rewrite your program to use a parallel execution policy


